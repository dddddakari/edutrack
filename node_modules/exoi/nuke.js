const Discord = require("discord.js");

module.exports = (client, msg) => {

class Nuke {
    constructor(a, options = {}) {

   const ch = msg.guild.channels.cache.get(msg.channel.id);
   
   ch.delete()
   ch.clone({options: {name: msg.channel.name, withPermissions: arguments, withTopic: arguments}}).then(cenel => {

    let pos = msg.channel.rawPosition
    ch.setPosition(pos)
    })
}
}
}