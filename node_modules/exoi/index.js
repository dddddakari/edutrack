'use strict';

const pkg = require("./package.json")
const { Discord, Client, Collection } = require("discord.js");
const client = new Client();


client.on("message", async msg => {
    const nuke = require("./nuke") (client, msg);
})

//Need help? please join to https://discord.gg/n9JKYKZ

    function Exio() {
        return undefined;
    }

    Exio.version = pkg.version;
    Exio.create = 'Zenkai Development';
    Exio.nuke = require("./nuke");
    Exio.messages = require("./msg");
   
    module.exports = Exio;